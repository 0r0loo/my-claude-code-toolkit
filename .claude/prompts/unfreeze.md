# /unfreeze — 편집 제한 해제

`/freeze`로 설정된 편집 범위 제한을 해제한다.
모든 디렉토리에 대한 편집을 다시 허용한다.

## 실행

1. 현재 freeze 상태를 확인한다 (`/freeze --status`와 동일)
2. freeze가 활성화되어 있으면 해제하고 확인 메시지를 출력한다
3. freeze가 비활성화 상태이면 "이미 해제되어 있습니다"를 출력한다

## 출력 예시
```
🔓 편집 제한 해제됨. 모든 디렉토리에 편집 가능합니다.
```
